execution:
  mode: "next_open"
  same_bar_stop_first: true
  commission_bps: 2.0
  slippage_bps: 5.0
  max_holding_days: 30
  max_participation_rate: 0.05
  min_fill_ratio: 0.25
  allow_partial_fill: true
  use_adjusted_price: false
  delisted_handling_policy: "ignore"
  missing_data_policy: "ignore"
metrics:
  annual_trading_days: 252
